program exercicio1;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils;

var
  result: Double;
  values: array[1..4] of Double;
  i: Integer;

begin
  try

    for i := 0 to 3 do
      WriteLn('Insira o valor de número [i]');
      Readln(values[i]);




  except
    on E: Exception do
      Writeln(E.ClassName, ': ', E.Message);
  end;
end.
